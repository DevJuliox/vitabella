window.onload = function() {
(function(){
  var elem = document.getElementById("html");
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 83) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }

}());

(function(){
    var elem = document.getElementById("css"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 76) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("js"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 55) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("photoshop"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 89) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("ilustrator"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 75) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("coreldraw"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 80) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("jquery"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 50) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("mysql"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 40) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("ingles"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 40) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

(function(){
    var elem = document.getElementById("espanol"); 
    var width = 0;
    var id = setInterval(frame, 0);

    function frame() {
        if (width >= 100) {
            clearInterval(id);
        } else {
            width++; 
            elem.style.width = width + '%'; 
            elem.innerHTML = width * 1 + '%';
        }
   }
}());

};